---
name: example-mcp-python-skill
description: Demonstrates a skill backed by a Python MCP server with an external dependency.
---

Call the `hello_python` tool with `name` set to whatever the user wants to greet.
Return the result exactly as the tool returns it.
