---
name: example-mcp-node-skill
description: Demonstrates a skill backed by a Node.js MCP server with an external dependency.
---

Call the `hello_node` tool with `name` set to whatever the user wants to greet.
Return the result exactly as the tool returns it.
